spring:
  application:
    name: Real_time_Leaderboard
  mongodb:
    uri: mongodb://localhost:27020/leaderboard

  data:
    redis:
      host: localhost
      port: 6379

api:
  security:
    token:
      secret: ${JWT_SECRET:my-secret-key}
